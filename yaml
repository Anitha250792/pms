services:
  - type: web
    name: productivity-management
    env: python
    buildCommand: "./manage.py collectstatic --noinput"
    startCommand: "gunicorn backend.wsgi:application"
    envVars:
      - key: DEBUG
        value: "False"
      - key: SECRET_KEY
        generateValue: true
      - key: MYSQL_DATABASE
        value: "productivity_db"
      - key: MYSQL_USER
        value: "root"
      - key: MYSQL_PASSWORD
        value: "root"
